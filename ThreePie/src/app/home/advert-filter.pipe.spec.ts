import { AdvertFilterPipe } from './advert-filter.pipe';

describe('AdvertFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvertFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
